<% if @level and @level.errors.any? %>
  <div class="alert alert-danger" role="alert">
    <ul>
      <% @level.errors.full_messages.take(5).each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
<%= form_for @level, :url => {:controller => "admin/levels", :action => action}  do |f| %>

  <div class="form-group input-group-lg">
    <%= f.label Level.human_attribute_name("name"), :class => "form-label"%>
    <%= f.text_field :name, autofocus: true, :class => "form-control" %>
  </div>

  <div class="form-group input-group-lg">
    <%= f.label Level.human_attribute_name("order"), :class => "form-label"%>
    <%= f.number_field :order, :class => "form-control" %>
  </div>

  <!--<button type="button" class="btn btn-default" data-dismiss="modal">Close</button>-->
  <div class="form-group">
    <%= f.submit  t('admin.submit') , :class => "btn btn-lg btn-primary btn-block", data: { disable_with: t('messages.sending') } %>
  </div>

  <p>
    <%= link_to t('admin.back'), admin_levels_path, :class => "text" %>
  </p>
 

<%end%>

